export type BuilderTranslations = {
  palette_title: string;
  canvas_title: string;
  config_title: string;
  canvas_empty: string;
  config_empty: string;
  preview_note: string;
  preview_error: string;
  download: string;
  copy_install: string;
  copy_install_done: string;
  apply_title: string;
  apply_step1: string;
  apply_step2: string;
  back: string;
  tab_palette: string;
  tab_canvas: string;
  tab_config: string;
  a11y_drag_start: string;
  a11y_drag_over: string;
  a11y_drag_end: string;
  a11y_drag_cancel: string;
};

export type CanvasSegment = {
  instanceId: string;
  defId: string;
  format: string;
  color: string;
  bold: boolean;
};
